#!/usr/bin/python
from kellerLD import KellerLD
import time
import os
from read_tsl import Tsl2591
from mpu6050 import mpu6050

debug=True

#Save data in data/pressure
directory = '/home/pi/camtag/tag_data/sensors/'
if not os.path.exists(directory):
        os.makedirs(directory)
text_file = open(directory + str(int(time.time())) + '.txt','w')
header="{0},{1},{2},{3},{4},{5},{6},{7}\n".format('UTC','Pressure','Temperature','Lux','IR spectrum','ax','ay','az','gx','gy','gz')
if debug:
	print header
text_file.write(header)

sensor = KellerLD()
if not 
